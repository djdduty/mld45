FILE(GLOB_RECURSE GLEW_SOURCES *.c)
FILE(GLOB_RECURSE GLEW_HEADERS *.h)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/include)

ADD_LIBRARY(MLD45Glew ${GLEW_SOURCES} ${GLEW_HEADERS})
SET_TARGET_PROPERTIES(MLD45Glew PROPERTIES LINKER_LANGUAGE C COMPILE_FLAGS "-DGLEW_NO_GLU")
